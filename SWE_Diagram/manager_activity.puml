@startuml
title Manager Management Activity Diagram

|Manager|
start
:Log In to System (Manager Account);
|System|
:Authenticate Login and Check Permissions;
if (Manager Authentication Successful?) then ([Yes])
    |Manager|
    :Access Manager Dashboard;
    :Select Task to Perform;
    if (Task?) then ([Order Management])
        partition "Order Processing (Staff Duty)" {
            :Check New Orders;
            :Accept/Reject Order;
            :Manage Cooking and Delivery Status;
            :Process Order Completion;
            :Return to Dashboard;
            -> :Select Task to Perform;
        }
    elseif ([Inventory/Finance Mgt])
        :Select Inventory/Finance Menu;
        if (Sub-task?) then ([Inventory Mgt])
            :View Inventory Status;
            :Register Stock In/Used/Disposed;
            |System|
            :Update Inventory DB Quantity;
            |Manager|
            :Return to Dashboard;
            -> :Select Task to Perform;
        else ([Finance Mgt])
            :View Sales Report (Daily/Weekly/Monthly);
            |System|
            :Aggregate Data and Generate Report;
            |Manager|
            :Check and Download Report;
            :Return to Dashboard;
            -> :Select Task to Perform;
        endif
    elseif ([Staff Management])
        :Select Staff Management Menu;
        :View Staff List;
        if (Sub-task?) then ([Register New])
            :Enter New Staff Info (ID, Permissions, etc.);
            |System|
            :Create Staff Account;
        else ([Edit/Delete])
            :Select Existing Staff;
            :Edit Info or Delete Account;
            |System|
            :Update/Delete Staff Info;
        endif
        |Manager|
        :Return to Dashboard;
        -> :Select Task to Perform;
    elseif ([Promotion Management])
        :Select Promotion Management Menu;
        if (Sub-task?) then ([Register New])
            :Enter Promotion Info (Period, Discount Rate, etc.);
            |System|
            :Create Promotion;
        else ([Edit/End])
            :Select Existing Promotion;
            :Edit or End Promotion Early;
            |System|
            :Update Promotion Status;
        endif
        |Manager|
        :Return to Dashboard;
        -> :Select Task to Perform;
    else ([Log Out])
        stop
    endif
else ([No])
    :Login Failed Message;
    stop
endif
@enduml